import 'package:flutter/material.dart';

ThemeData lightTheme = ThemeData(
    brightness: Brightness.light,
    primaryColor: const Color(0xffe12b2b)
);

ThemeData darkTheme = ThemeData(
    brightness: Brightness.dark
);